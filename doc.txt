Personal doc

		** PUSH **
 -	line with val
push to stack
$	"Hello world!"
$	42
$	6.9


		** OP **
 -	#
ouput number of input elem

 -	!
drop last stack elem


		** MOD **
modifier (MOD) add event to OP
$	MODOP

 -	#
pop first elem stack, then, repeat OP int times
$	2#!
// pop first elem stack
// repeat OP "!" 2 times (delete 2 last elem stack)

 -	%


		** IF / WHILE **
 -	?
if
 -	? OP : OP
if + else (ternary)

 -	@
while (first elem stack == true)


		** SUBSET / FUNCTION **
 -	{ OP } function
create a function
$	{ print "\n" print } print_with_new_line
// print last stack
// add "\n" to stack
// print last stack ("\n")
$	print_with_new_line
// call function

 -	( OP )
create a subset
